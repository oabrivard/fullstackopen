{
  "name": "notes-backend",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "jest --verbose",
    "lint": "eslint .",
    "build:ui": "rm -rf build && cd ../../../fullstackopen/part2/notes && npm run build --prod && cp -r build ../../../fullstackopen/part3/notes-backend/"
  },
  "author": "Olivier Abrivard",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "mongoose": "^5.9.27"
  },
  "devDependencies": {
    "eslint": "^7.6.0",
    "jest": "^26.4.0",
    "nodemon": "^2.0.4"
  },
  "jest": {
    "testEnvironment": "node"
  }  
}
